import sys,os,argparse,shutil,glob,json,copy,timeSLOTS = ['route','from.desc','from.neighborhood','from.monument','to.desc','to.neighborhood','to.monument','date','time']install_path = os.path.abspath(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))utils_dirname = os.path.join(install_path,'lib')list_dir = os.path.join(install_path,'config')sys.path.append(utils_dirname)from dataset_walker import dataset_walker    	def main(dataset, dataroot):	sessions = dataset_walker(dataset,dataroot=dataroot,labels=True)		slen = 0	for session in sessions:		slen = slen + 1		print slen	if (__name__ == '__main__'):		if len(sys.argv) != 2:		dataset = "test2"		dataroot = "../../data"	else:		dataset = sys.argv[1]		dataroot = sys.argv[2]		main("test1", dataroot)	#main("test2", dataroot)	#main("test3", dataroot)	#main("test4", dataroot)	#main("train1a", dataroot)	#main("train2", dataroot)	#main("train3", dataroot)	print "Done"